on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Building cc26xx-web-demo
    steps:
    - name: Build Contiki example project
      id: contiki
      uses: xanderhendriks-uts/action-build-contiki@master
      with:
        which-project: 'cc26xx-web-demo'